version: "3"

services:
  maxscale:
    container_name: {{ container_name }}
    image: {{ maxscale_image }}
    ports:
      - 8989:8989 #RestApiPort
    volumes:
      - /opt/maxscale/maxscale.cnf:/etc/maxscale.cnf
    restart: always
